stores:
  description: |
    A list of store objects, where each store object may contain the
    following fields:

    - ``id``
    - ``description``
    - ``default``
    - ``read-only``
  in: body
  required: true
  type: array
